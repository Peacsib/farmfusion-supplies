/* ===== ENTERPRISE MOTION — scroll-driven animations ===== */
document.addEventListener('DOMContentLoaded', () => {
    // Scroll reveal (delegated to main.js IntersectionObserver)
    // Hero parallax (delegated to main.js)
    // This file intentionally kept minimal — logic lives in main.js
});
